# Sprite sheet definition for "debug_npc_8way"
# Generated by gen_debug_textures.py

frame_width = 64
frame_height = 64
facings = 8
frame_rate = 6    # ticks per animation frame

# States — one section per visual state.
# "frames" is the number of animation columns for that state.
# Row range in the sheet: [state_index * facings .. (state_index+1) * facings - 1]

[states.default]
frames = 4
row_start = 0    # rows 0..7

[states.aggro]
frames = 2
row_start = 8    # rows 8..15

[states.dead]
frames = 1
row_start = 16    # rows 16..23

