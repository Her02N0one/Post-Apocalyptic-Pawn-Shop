"""engine — Low-level rendering infrastructure.

Modules
-------
raycaster       Pure-Python DDA wall-casting + C-accelerated fast path
ray_renderer    Buffer management & C extension wrapper for frame rendering
textures        Tile texture atlas loading / caching (PNG-only)
"""
