# Brick Wall — Full-height solid column with brick textures.
# Both heights at 0 → no gap → solid wall column.
# Scroll-down in sculpt mode to open it back up.

name = "Brick Wall"
category = "Walls"
color = [140, 80, 60]

apply_mode = "replace"

floor_height = 0.0
ceil_height = 0.0

wall_texture = "brick_wall"
face_textures = ["brick_wall", "brick_wall", "brick_wall", "brick_wall"]
